# Codex Project Guide

이 저장소는 JD 맞춤 지원서 자동화 작업 공간이다.

목표는 JD 하나를 입력하면 `분석 -> 전략 -> 맞춤 이력서/포트폴리오 -> PDF` 흐름을 같은 기준으로 반복하는 것이다. Codex 공식 문서 기준 프로젝트 지침은 루트 `AGENTS.md`에 두고, 세부 작업 절차는 `.agents/skills/*/SKILL.md`에 둔다.

## Git Tracking Policy

- GitHub에는 스킬, `.gitignore`, `AGENTS.md`, 공개 템플릿만 올린다.
- `db/`, `workflow/`, `assets/`는 개인정보와 지원 산출물을 담는 로컬 전용 경로이므로 Git에 올리지 않는다.
- 로컬 `db/`는 지원 현황과 Notion 원천 스냅샷 보조자료로 계속 사용하지만 버전관리 대상이 아니다.

## Canonical Files

- 이력서/포트폴리오 원천 DB: Notion `박건우 | Developer` 페이지
  - URL: `https://confused-dietician-c17.notion.site/Developer-3c77caa087bd80ff9d73f63aeeebb4d7?pvs=74`
- 로컬 원천 스냅샷(보조 참고): `db/이력서_원천.md`, `db/포트폴리오_원천.md`
- 지원회사 관리 DB: `db/지원회사_관리.md`
- 공용 포트폴리오 자산: `assets/portfolio/`
- 이력서 기준 템플릿: `templates/public/resume.typ`
- 포트폴리오 기준 템플릿: `templates/public/portfolio.typ`
- JD 분석 스킬: `.agents/skills/company-jd-analyzer/SKILL.md`
- 지원서 생성 스킬: `.agents/skills/cv-jd-tailor/SKILL.md`

## Core Workflow

1. JD를 먼저 읽고 회사명, 직무명, JD 식별자를 확정한다.
2. `company-jd-analyzer`로 회사/JD 분석을 수행한다.
3. 분석 결과는 `workflow/지원전/{회사명}/{회사명}_채용분석.md`에 둔다.
4. `cv-jd-tailor`로 Notion 원천 DB를 먼저 읽고 지원전략, Typst, PDF를 생성한다.
5. 지원 전 산출물은 `workflow/지원전/{회사명}/`에 둔다.
6. 지원 후 보관본은 사용자가 명시하거나 상태 변경이 필요한 경우에만 `workflow/지원후/{회사명}/`에 둔다.
7. 지원 상태는 `db/지원회사_관리.md`에서 중복 없이 갱신한다.
8. 지원 현황은 현재 상태만 관리한다. 변경 이력 섹션이나 별도 상태 로그는 만들지 않는다.

## Ground Rules

- 경험 사실, 수치, 역할, 성과는 Notion `박건우 | Developer` 원천 페이지와 사용자 최신 정정만 근거로 사용한다. 로컬 md 스냅샷은 Notion 접근이 불가능한 경우의 보조 참고로만 사용하고, Notion과 충돌하면 Notion을 우선한다.
- Notion 원천은 최종 문장이 아니라 재료다. JD에 맞춰 표현, 순서, 강조점은 바꾸되 사실관계와 인과관계는 새로 만들지 않는다.
- 서로 다른 경험 사실을 임의 인과관계로 합치지 않는다.
- 여러 경험, 프로젝트, 교육 과제, 도구 사용 기록을 애매하게 붙여 하나의 기술 구현, 하나의 성과, 하나의 직무 경험처럼 보이게 쓰지 않는다. 각 문장은 같은 프로젝트 안에서 실제로 이어진 문제-실행-결과만 묶고, 출처가 다른 경험은 별도 불릿이나 별도 항목으로 나눈다.
- 사용자가 명시하지 않은 동기, 문제의식, 의도, 신념을 이력서/포트폴리오 문장으로 만들지 않는다. 특히 `~라는 문제의식으로`, `~하겠다는 의도로` 같은 표현은 Notion 원천이나 사용자 최신 정정에 근거가 있을 때만 쓴다.
- 직접 경험이 없는 기술명은 보유처럼 쓰지 않는다.
- 내가 직접 한 일과 라이브러리·프레임워크·클라우드 서비스가 제공한 기능을 절대 혼동하지 않는다. 예를 들어 Yjs/Monaco/Bedrock/API Gateway/SQS/OpenSearch가 제공한 기본 기능은 `사용/연동/구성`으로 쓰고, 내가 작성한 화면, API 계약, 상태 처리, 예외 처리, 데이터 흐름, UX 개선, 검증 범위만 `구현/개선/설계`로 쓴다.
- 팀 전체가 한 일, 라이브러리가 자동으로 처리한 일, 프레임워크 기본 동작, 공식 서비스 내장 기능을 내가 직접 구현한 것처럼 쓰지 않는다. 필요하면 `도입`, `연동`, `설정`, `활용`, `화면에 연결`, `상태를 표시`, `예외 케이스 처리`처럼 실제 수행 범위를 드러내는 동사를 쓴다.
- 도메인이 포함된 제품을 개발한 경험과 그 도메인의 운영 실무를 직접 수행한 경험을 혼동하지 않는다. 예를 들어 프비티에서 CRM 마케팅 기능이 포함된 프랜차이즈 업무 플랫폼을 개발한 사실은 쓸 수 있지만, 광고·마케팅 운영 업무를 직접 담당하거나 개선했다고 쓰면 안 된다.
- 도메인 인접 경험을 어필할 때는 `마케팅 운영을 했다`처럼 넓게 쓰지 말고 `CRM 마케팅 기능이 포함된 업무 플랫폼에서 사용자 화면과 API 흐름을 구현했다`처럼 실제 접점과 수행 범위를 좁혀 쓴다.
- 서로 다른 확인 사실을 합쳐 새 기술명처럼 쓰지 않는다. 예: Bedrock Agent Trace와 API Gateway WebSocket 근거가 따로 있어도 `WebSocket Trace`처럼 합성하지 말고, 각각 `Bedrock Agent Trace`, `API Gateway WebSocket`, `진행 상태 UI`처럼 확인된 단위로 쓴다.
- `초안`, `확인 필요`, `실제 반영 범위 확인 필요`로 표시된 기술은 구현 완료처럼 쓰지 않는다. 공고 대응에는 `검토`, `설계`, `PoC`, `학습` 중 확인된 수준으로 낮춰 표현하거나 제외한다.
- 이력서 불릿은 생각등대 기준을 따른다. 단순 기술 나열이 아니라 `어떤 문제가 있었는지`, `왜 그 방식이 필요했는지`, `무엇을 적용했는지`, `결과가 무엇인지`를 면접관이 이해할 수 있는 문장으로 쓴다.
- 이력서는 경력 보관용 연표가 아니라 면접 호출을 만드는 판단 문서다. 첫 10~15초 훑어보기에서 현재 직무 적합성, 가장 강한 실무 근거, 기술 깊이, 리스크가 없는 표현이 보여야 한다.
- 불릿은 `담당했습니다`보다 `무엇이 달라졌는지`를 보여준다. 가능하면 범위, 수치, 사용자/업무 영향, 검증 결과를 넣고, 수치가 없으면 코드 규모, API 수, 제약 조건, 처리 시간, 화면/상태/예외 범위처럼 검증 가능한 스코프를 쓴다.
- `AX 전환에 기여`, `운영화`, `아키텍처 설계`처럼 큰 표현은 단독으로 쓰지 않는다. 반드시 같은 문장이나 바로 다음 문장에서 다중 Agent 구성, 진행 상태 UI, API/데이터 흐름, SQS 비동기 처리, 보안 키 분리, 검색 UX처럼 내가 실제로 설계·개발·연동한 하위 근거를 붙인다.
- 문장은 자연스럽고 읽는 사람이 쉽게 이해할 수 있어야 한다. 내부 약어, 기술명, 도메인명을 그대로 던지지 말고 `누가 쓰는 서비스인지`, `어떤 업무를 줄였는지`, `내가 어떤 화면/API/상태/데이터 흐름을 만들었는지`를 한 문장 안에서 설명한다.
- 쉽게 쓰되 기술적으로 얕아지면 안 된다. 기술명만 나열하지 말고, `왜 그 기술이 필요했는지`, `라이브러리/서비스를 어떻게 연동했는지`, `내가 처리한 예외·상태·성능·보안·검증 범위가 무엇인지`를 드러낸다.
- 지원서 문체는 자연스럽고 겸손해야 한다. `증명하겠습니다`, `~를 전환하는 개발자`, `역량을 보유했습니다`처럼 AI가 쓴 듯한 선언형 문장은 피하고, `~을 구현했습니다`, `~한 경험이 있습니다`, `~을 연결해 봤습니다`, `~을 맡았습니다`처럼 사실 중심으로 쓴다.
- 지원서 본문에 `제외 기술`, `직접 근거가 약해`, `보유 기술처럼 쓰지 않는다` 같은 방어적 메타 문구를 넣지 않는다. 이런 판단은 전략 문서에만 남기고, 제출용 이력서/포트폴리오에서는 검증된 경험만 자연스럽게 선택한다.
- JD의 `담당업무`, `자격요건`, `우대사항` 문구를 우선하되, 실제 경험과 의미가 맞는 범위에서만 사용한다.
- 자격증은 공고가 명시적으로 요구하거나 우대할 때만 쓴다. 명시가 없으면 SQLD 같은 자격은 제출용 이력서에서 제외한다.
- 어학은 사용자가 명시적으로 넣으라고 지시한 기본 신뢰 정보이므로 하단 `수상 및 어학`에 짧게 포함한다. 현재 기준은 `2026.07.01 OPIc IM2`만 쓰고 인증번호나 유효기간은 제출용 이력서에 쓰지 않는다.
- 분석 문서의 사실/추론 라벨(`[명시]`, `[최근근거]`, `[추정]`, `[확인필요]`)은 전략 문서에도 유지한다.
- `workflow/지원후/`는 제출·보관본이다. 사용자가 명시적으로 요청한 경우에만 수정하거나 삭제한다.
- `workflow/지원전/`은 작업 산출물 위치다.
- 산출물 파일명은 `{회사명}_{산출물명}_{지원자명}` 형식을 기본으로 한다.
- Typst 산출물은 `.typ`와 `.pdf`를 기본으로 한다.
- Typst 본문에서 기간·범위의 물결표는 반드시 보이는 문자로 남긴다. `1~2분`, `3~5주`, `2024.02.07~2024.02.25`처럼 범위를 뜻하는 `~`는 Typst에서 `1\~2분`, `3\~5주`, `2024.02.07\~2024.02.25`처럼 이스케이프해 PDF에서도 물결표가 사라지지 않게 한다. 범위 표기를 임의로 하이픈이나 공백으로 바꾸지 않는다.

## Folder Policy

- 회사별 맞춤 산출물은 회사 폴더에 둔다.
- 반복 사용 가능한 사실은 회사 폴더에 복제하지 말고 `db/`에 남긴다.
- 반복 사용 가능한 포트폴리오 이미지는 `assets/portfolio/`에 둔다.
- 회사별로만 쓰는 제출 첨부파일은 해당 회사 폴더에 둔다.
- `workflow/outputs/`나 분기별 `{YYYY-qN}/` 경로는 쓰지 않는다. 현재 기준 경로는 `workflow/지원전`과 `workflow/지원후`다.

## Portfolio Assets

공용 포트폴리오 이미지는 아래 위치를 사용한다.

- 아키텍처 이미지: `assets/portfolio/architecture/`
- 컴포넌트 이미지: `assets/portfolio/components/`
- 화면 캡처: `assets/portfolio/screenshots/`
- 흐름/시퀀스/프로세스 도식: `assets/portfolio/diagrams/`
- 로고: `assets/portfolio/logos/`

이미지를 포트폴리오에 사용할 때는 같은 이미지를 회사 폴더마다 복사하지 않는다. 생성된 Typst 파일에서는 회사 폴더 기준 상대 경로를 사용한다.

## Template Rules

- 이력서 기본 템플릿의 단일 기준은 `templates/public/resume.typ`다.
- 포트폴리오 기본 템플릿의 단일 기준은 `templates/public/portfolio.typ`다.
- `workflow/지원전/_shared_application.typ`와 `workflow/지원전/_fixed_portfolio_cases.typ`가 있으면 기존 산출물 호환용으로 사용할 수 있다.
- 회사별 맞춤은 내용, 순서, 강조점, 불릿 선별로 처리한다.
- 기본 레이아웃을 바꿀 때는 템플릿 파일을 수정하고, 회사별 산출물마다 레이아웃을 새로 만들지 않는다.
- 이력서에서 재직 중 수행한 회사 업무는 `경력` 섹션에만 녹인다. 같은 내용을 `주요 프로젝트`에 중복 배치하지 않는다.
- 한 회사 경력 안에 여러 제품, 프로젝트, 업무영역이 섞여 있으면 경력 항목 내부에서 작은 소제목으로 나눈다. 회사 업무를 `주요 프로젝트`로 빼지 않는다.
- 디지엠유닛원 경력 내 프로젝트 구분은 반드시 `LG 전 계열사가 사용하는 공통업무 플랫폼(CPPM)`과 `프비티` 두 개의 작은 소제목으로 나눠 적는다. 하나의 불릿 목록에 CPPM, 프비티, AI Native Workflow를 섞지 않는다. AI Native 워크플로우는 별도 프로젝트로 세우지 않고 실제 수행 프로젝트 안의 업무 방식/성과로 녹인다.
- CPPM의 프로젝트/제품 설명에는 정확한 설명으로 `LG 전 계열사가 사용하는 공통업무 플랫폼`을 쓴다. 다만 자기소개, 한줄소개, 상단 강점 문구, 지원동기처럼 지원자 포지셔닝을 말하는 영역에서는 고객사 실명을 빼고 `대기업 모든 계열사가 사용하는 공통업무 플랫폼`, `외부 LLM 사용이 제한된 환경의 AI 업무지원 기능`, `AI Agent 기반 업무지원 기능`처럼 감싸서 쓴다. `대기업 계열사 공통 업무 플랫폼`처럼 범위가 어색한 표현은 쓰지 않는다.
- CPPM/AX 강점을 쓸 때는 `Bedrock Agent, SQS, WebSocket 진행 상태 UI를 연결`, `외부 LLM 사용이 제한된 환경에서 AI 기능을 업무 화면으로 구현`처럼 사용자가 지정한 안전 표현을 우선한다. 자기소개/한줄소개에서는 회사명보다 보안 제약, 업무 플랫폼, AI Agent, 화면/API/비동기 처리 연결 경험을 앞세운다.
- 프비티 설명은 공식 사이트 기준 `프랜차이즈 본사를 위한 통합 올인원 솔루션`으로 쓴다. 필요하면 멤버십, 수발주, 재고 관리, 가맹점관리, CRM 마케팅을 통합 관리하는 서비스라고 풀어 쓴다.
- 프비티 분리 경험은 `서비스 분리`, `독립 서비스`, `단독 판매·운영`, `단독 실행`, `233곳 완전 제거`처럼 완료 범위가 커 보이는 말로 쓰지 않는다. 사용자 최신 정정 기준으로, 수발주·영업관리·계정관리 기능이 강하게 결합된 구조에서 기능 영역을 별도 프로젝트 구조로 나누고 각 프로젝트에 필요한 계정관리 흐름을 정리하기 위해 36K LOC, 300+ files, 39 APIs와 본사/브랜드 식별자 의존성 제거·치환 범위를 분석한 경험으로 쓴다. 외부 제출 문서에는 `cvve-tenant`, `cvve-sales`, `hqID`, `brandID` 같은 내부 코드명/내부 식별자명을 단독으로 쓰지 않는다.
- 경력과 주요 프로젝트 항목은 반드시 `프로젝트/제품 이름 -> 기술 스택 -> 프로젝트/제품 소개 -> 불릿` 순서로 쓴다. 기술 스택과 소개는 프로젝트 이름 바로 하위에 배치하고, 불릿 사이에 섞지 않는다.
- 프로젝트/제품 소개는 기술 스택 바로 아래에 같은 계열의 작은 크기·옅은 색상으로 붙인다. 소개는 1문장, 가능하면 40자 안팎으로 짧게 쓰고 서비스 성격만 알려준다. 상세 배경, 문제, 성과는 소개에 넣지 말고 불릿로 보낸다.
- 기술 스택은 사용해 본 기술 전체 목록이 아니라 JD 매칭 증거다. `담당업무`, `자격요건`, `우대사항`을 분석해 공고와 직접 맞거나 유사 역량으로 설명 가능한 기술만 앞세운다. 공고와 관련 없는 기술은 실제 사용했더라도 빼거나 불릿 본문 맥락에만 최소로 둔다.
- 기술 스택 줄에는 공식 문서나 제품 페이지에서 쓰는 기술/제품명만 적는다. 역할명, 프로젝트 성격, 업무 설명, 성과, 과정명, `AI Native Workflow` 같은 작업 방식, `Software Engineer` 같은 직무명은 기술 스택에 넣지 않는다.
- 공식 명칭이 불확실한 기술은 쓰기 전에 공식 문서, 제품 페이지, README, 패키지명을 확인한다. 클래스명, 내부 구현 객체명, 임의 축약어는 기술스택 줄에 넣지 않는다.
- 기술스택 나열 순서는 무작위로 두지 않는다. JD에서 가장 먼저 보여야 하는 핵심 기술을 앞에 두고, 같은 중요도라면 `주요 프레임워크/라이브러리 -> 언어/상태관리 -> UI/스타일링 -> 실시간·협업·도메인 기술 -> 빌드/배포·인프라` 순서로 정리한다.
- AWS 서비스는 공식 제품명 기준으로 쓴다. 예: `Amazon Bedrock`, `Knowledge Bases for Amazon Bedrock`, `AWS Lambda`, `Amazon API Gateway WebSocket APIs`, `Amazon SQS`, `Amazon DynamoDB`, `Amazon OpenSearch Service`.
- 프로젝트별 기본 기술스택은 원천/사용자 정정 기준을 우선한다. 프비티는 `Next.js, React, styled-components, Vite, Spring Boot, MySQL, Docker`, CPPM은 `React, styled-components, Vite, Docker, AWS`, Code Sync는 `React, Tailwind CSS, Yjs, WebSocket, WebRTC, Vite`를 기본 후보로 쓰고, JD와 맞지 않는 항목만 줄인다.
- 공고 성격에 따라 기술스택의 상세도를 조절한다. 프론트엔드 공고에서는 DB, 백엔드, AWS 세부 서비스를 불필요하게 앞세우지 않는다. AI/FDE/자동화/AWS 공고에서는 CPPM의 `AWS`를 `Amazon Bedrock`, `AWS Lambda`, `Amazon SQS`, `Amazon DynamoDB`처럼 핵심 서비스 3~4개로 풀어 쓸 수 있다.
- AWS 세부 서비스가 기술스택에 너무 길게 들어가면 주요 3~4개만 기술스택에 두고, `Knowledge Bases for Amazon Bedrock`, `Amazon API Gateway WebSocket APIs`, `Amazon OpenSearch Service`, `Amazon CloudWatch` 같은 세부 구성은 불릿에서 설명한다.
- `주요 프로젝트` 섹션은 프로젝트별로 명확히 구분한다. 프로젝트명, 기간, 역할, 기술 스택, 문제-해결-결과 불릿이 서로 섞이지 않게 작성한다.
- `주요 프로젝트` 섹션은 경력 외 개인 프로젝트, 교육 과정 최종 프로젝트, 연구/캡스톤 등 일 외 산출물만 배치한다.
- SKALA는 크래프톤 정글처럼 교육이다. `SKALA 4기 실습` 같은 뭉뚱그린 프로젝트명으로 주요 프로젝트에 세우지 않는다.
- SKALA의 공식 설명은 `SK AI Leader Academy`, `AI 서비스 개발 과정`, `SW, Data·AI 교육 및 팀 프로젝트` 수준으로만 쓴다. 공식 출처에 없는 `백엔드·클라우드 네이티브 과정` 같은 과정명은 쓰지 않는다.
- SKALA는 공고와 직접 맞을 때만 `교육/활동`에 1~2개 불릿로 짧게 둔다. MSA, Spring Cloud, Kafka, JPA, PostgreSQL, Docker, 쿼리 튜닝, 인덱스/실행계획, 트랜잭션, 동시성, 테스트 같은 기술명은 공식 과정명처럼 쓰지 말고, 로컬 README·코드·실행 결과로 확인된 개별 과제/학습 근거로만 쓴다.
- SKALA web-mini의 서비스 주제인 공개 재판, AI 판결, 커뮤니티 투표 같은 도메인/장난감 서비스 설명은 지원서에서 강조하지 않는다. 필요하면 MSA/API/STOMP/쿼리/테스트/배포 등 기술 요소만 남긴다.
- 크래프톤 정글은 CS 집중 교육으로 표시한다. CSAPP, C, 운영체제, 네트워크, 자료구조·알고리즘 학습과 최종 프로젝트 Code Sync의 프론트엔드 역량이 IT 지원공고 대부분에 유효하므로, JD와 충돌하지 않는 한 Code Sync는 주요 프로젝트 후보에 우선 포함한다.
- Code Sync를 `주요 프로젝트`로 쓰더라도 크래프톤 정글 자체는 `교육/활동`에 별도 항목으로 남긴다. 단, 교육 불릿은 보통 1개로 압축하고 `주 100시간 이상 전산학을 학습하며`로 시작하는 흐름을 기본으로 한다. CSAPP, malloc, mmap, Tiny Web Server, Pintos Thread/User Program/Virtual Memory/File System처럼 전산학 기반을 보여주는 사실만 한 문장에 묶는다. Pintos는 기술스택이 아니라 교육용 운영체제 과제이므로 기술스택 줄에 넣지 않고, 백엔드·인프라 실무 경험처럼 연결하지 않는다.
- `크래프톤 정글 우수 프로젝트 선정`은 어떤 공고에서도 수상/성과로 넣지 않는다. Code Sync는 프로젝트 구현 경험으로만 사용한다.
- 군 복무는 교육/활동에 짧게 포함한다. 통신병·또래상담병 사실만 쓰고, 통신 장비 개발이나 임베디드 실무처럼 확장하지 않는다.
- 수상은 대학 프로젝트를 핵심 어필에서 제외하더라도 하단 `수상 및 어학` 섹션에 사실 정보로 남긴다. 날짜순 불릿로 `2023.07 ICICT 2023 치앙마이 포스터상`, `2023.08 조선대학교 IT융합대학 캡스톤디자인 경진대회 은상`, `2024.02 총장배 모범상`을 쓴다. 수상 경위나 프로젝트 연결이 확인필요인 항목은 설명을 덧붙이지 않는다.
- 아티피셜소사이어티는 `2024.02.07~2024.02.25` 3주 단기 인턴으로 교육/활동에 포함한다. 기본 표현은 AI 시선추적 기반 서비스의 `모델 성능 향상 지원`이며, 세부 설명이 필요할 때만 Data Augmentation, EasyOCR 품질 개선 지원처럼 확인된 범위로 좁혀 쓴다. `데이터 지원`처럼 넓은 라벨로 쓰지 않는다.
- 엘리스와 스마일게이트 윈터데브는 기본적으로 이력서/포트폴리오에 넣지 않는다. 사용자가 해당 공고에서 명시적으로 요구하거나 최신 정정으로 쓰라고 한 경우에만 검토한다.
- 네이버 부스트코스/부스트캠프 코칭스터디 이력은 어떤 공고에서도 제출용 이력서 기본 항목으로 쓰지 않는다. 사용자가 특정 공고에서 명시적으로 넣으라고 한 경우가 아니면 `Python·데이터·AI 학습은 네이버 부스트코스/부스트캠프 코칭스터디로 이어졌습니다` 같은 문장은 제거한다.
- 디지엠유닛원 경력 역할명은 JD에 맞춰 조정한다. 프론트엔드 공고는 `Frontend Engineer`, 풀스택/AI Native/AX/AI 서비스/PI/Builder 성격 공고는 `Software Engineer`로 쓴다. 이 규칙은 디지엠유닛원 재직 경력에만 적용하며, 크래프톤 정글 Code Sync는 크래프톤 정글 교육 과정의 최종 프로젝트이지만 프로젝트 역할은 `Frontend Developer`로 둔다.
- 이력서 경력 불릿은 소개나 서브타이틀에 이미 나온 담당 범위를 반복하지 않는다. 문제, 실행, 성과/수치/도메인 임팩트가 없는 문장은 소개·서브타이틀 맥락으로만 두거나 제거한다.
- 제출용 이력서의 `지원동기`는 공고가 요구하거나, 회사/직무와 내 관심사·서사·경험이 자연스럽게 연결될 때만 둔다. 억지로 개인 서사를 끼워 맞추지 말고, 연결이 약하면 섹션을 제외한다. `소개`는 공고나 입력 양식이 `간단한 자기소개`, `자기소개`, `About` 등을 명시적으로 요구할 때만 추가한다.
- 지원동기는 회사 칭찬이나 호감 표현이 아니라 `이 회사의 이 직무 문제가 내가 실제로 해 온 문제 해결 방식과 맞는 이유`를 설명하는 섹션이다.
- 지원동기의 공용 구조는 `회사/직무에서 본 구체적인 문제나 일의 성격 -> 그 지점이 내 경험/관심사와 연결되는 이유 -> 실제로 해 본 유사 경험 1~2개 -> 입사 후 내가 어떤 방식으로 일할 수 있는지` 순서로 쓴다.
- 지원동기에서도 도메인 경험을 과장하지 않는다. 해당 도메인의 제품/기능을 개발한 경험은 `인접 경험`으로만 연결하고, 운영 실무를 직접 수행한 것처럼 쓰지 않는다.
- 지원동기를 쓸 때 가장 중요한 중심은 `왜 하필 이 회사인지`다. 회사의 비전, 가치, 사업 방향, 제품/서비스, 최근 행보 중 실제 출처로 확인한 내용을 내 관심사·서사·역량과 자연스럽게 엮어 그 회사에만 해당하는 이유를 만든다. 그 다음에 `왜 이 팀인지`, `왜 이 직무인지`, 내가 이 회사에서 어떤 일을 할 수 있는지를 연결한다. 회사명을 바꿔도 통하는 문장이면 지원동기로 쓰지 않는다.
- 지원동기는 JD 요약문처럼 쓰지 않는다. `역할로 이해했습니다`, `반복 업무와 병목을 이해하고`, `운영 가능한 솔루션으로 만드는 역할`처럼 공고를 풀어쓴 문장만 있으면 약하다. 회사/팀/직무 맥락을 한 문장으로 짚은 뒤, 바로 내가 해 본 유사 업무와 그 역할에서 할 수 있는 일을 말한다.
- 지원동기 결말은 `기여하고 싶습니다`, `성장하고 싶습니다`, `역량을 발휘하겠습니다`, `비전에 공감했습니다` 같은 희망형 문장으로 끝내지 않는다. 마지막 문장은 내가 해당 역할에서 실제로 할 수 있는 일과 일하는 방식으로 끝낸다.
- 포트폴리오 요약도 지원동기와 같은 기준으로 쓴다. 첫 문장을 JD 설명이나 `공고는 ~를 요구합니다`로 시작하지 말고, 내가 실제로 만든 것과 검증된 강점을 먼저 제시한다. JD와의 연결은 그 다음 문장에서 짧게 붙이고, 도메인 인접 경험은 `직접 수행`과 `제품 개발 접점`을 분명히 나눈다.
- 포트폴리오 사례는 이력서 반복이 아니라 면접 질문을 만드는 기술 사례여야 한다. 각 사례는 문제/제약, 내가 맡은 판단과 구현 범위, 사용한 기술을 선택한 이유, 결과/검증, 남은 리스크 또는 배운 점 중 최소 3개 이상이 드러나야 한다.
- 포트폴리오는 `/Users/baggeon-u/Desktop/cv-jd-tailor/박건우_포트폴리오.pdf`처럼 기술적으로 읽혀야 한다. 단순 요약이나 이력서 반복이 아니라 `문제/제약 -> 구조/흐름 -> 코드 또는 구현 포인트 -> AI 활용 방식 -> 검증/결과`가 보여야 한다.
- 회사별 맞춤 포트폴리오의 경험 풀은 기본적으로 디지엠유닛원, 크래프톤 정글, SKALA만 사용한다. 우선순위는 디지엠유닛원 경험이며, 공고 요구와 직접 맞는 근거가 부족할 때만 크래프톤 정글 Code Sync 또는 SKALA 과제/학습을 보조 사례로 쓴다. 조선대 연구, 아티피셜소사이어티, 부스트코스, 엘리스, 스마일게이트 등은 사용자가 명시하지 않으면 포트폴리오 대표 사례로 쓰지 않는다.
- 이력서/포트폴리오의 어필 우선순위는 `디지엠유닛원 실무 경험 -> 인턴 경험 -> 부트캠프/교육 프로젝트 -> 대학 프로젝트`다. 대학 프로젝트는 대부분 실무 어필력이 약하므로 핵심역량이나 대표 프로젝트에 올리지 않는다. 특히 `AI 추론 결과 서비스화`처럼 실제 운영 서비스처럼 보이는 표현은 쓰지 않는다. AI/헬스케어 공고라도 Amazon Bedrock 기반 AI 업무지원 기능을 화면·API·비동기 처리 흐름으로 연결한 디지엠유닛원 경험을 우선 사용한다.
- 포트폴리오에는 가능한 한 각 사례마다 코드 관점의 구현 포인트를 넣는다. 예: `connectionId` 기반 WebSocket 전송, Bedrock completion stream에서 text chunk와 Trace 이벤트 분리, `AgentEventManager` 싱글톤 구독/cleanup, `requestAnimationFrame` 스크롤 제어, `LastEvaluatedKey` 커서 페이지네이션, DOMPurify Sanitization, Lambda 환경변수 기반 API Key 격리, Graph Agent Lambda subprocess/S3 URL 반환.
- 이미지 자산이 없더라도 포트폴리오에는 구조가 보이도록 `Architecture / Flow` 시각 블록을 둔다. 실제 이미지 파일이 있으면 `assets/portfolio`를 우선 사용하고, 없으면 Typst 박스/그리드로 `Frontend -> API Gateway WebSocket -> Lambda -> Bedrock Agent -> SQS/DynamoDB/S3` 같은 흐름도를 텍스트 다이어그램으로 만든다.
- AI 활용 방법은 별도 과장 없이 실제 사용 맥락만 쓴다. 디지엠유닛원 공통 역량에서는 Figma MCP, Cursor, Codex, Skills를 퍼블리싱, 기술 조사, PoC, 문서화에 도입한 방식과 Plan-First Workflow, Human Approval Gate, Researcher·Planner·Reviewer 역할 분리, 프롬프트/로깅 체계를 기술한다. AI가 만든 결과를 본인이 직접 구현·검증한 것처럼 혼동하지 않는다.
- 포트폴리오 트러블슈팅은 최소 5개 이상으로 구성한다. 기본 후보는 `장시간 Agent 응답 대기 경험`, `채팅 검색 및 탐색`, `다중 Agent 아키텍처`, `API Gateway timeout/SQS 비동기 구조`, `API Key 보호/XSS 대응`, `AI 기반 개발 프로세스 내재화`다. 공고와 맞지 않는 경우에도 디지엠유닛원 내 다른 실제 문제 해결 사례로 대체한다.
- 아키텍처 이미지를 만들거나 사용할 때는 AWS 공식 Architecture Icons 또는 사용자가 제공한 이미지 자산만 사용한다. 공식 아이콘을 확보하지 못한 상태에서는 비공식 아이콘을 만들지 말고, Typst 텍스트 다이어그램으로 대체한다.
- 한줄소개는 일반적인 `~한 개발자` 선언보다 검증된 차별점을 먼저 보여준다. AX/AI/FDE 공고에서는 `보안 제약이 있는 업무 플랫폼에서 AI Agent 기능을 화면·API·비동기 처리까지 구현한 개발자`처럼 보안 제약, 업무 플랫폼, AI Agent, 화면/API/비동기 연결 경험을 묶어 쓴다. 고객사 실명이 민감한 영역에서는 회사명을 빼고, 실제로 구현·연동한 범위만 드러낸다.
- 이력서 첫 페이지는 면접관의 판단 화면이다. 첫 페이지 안에 JD와 맞는 나만의 강점, 가장 강한 경력/프로젝트 근거, 회사가 뽑아야 할 이유가 보여야 한다. 첫 페이지를 일반 소개나 넓은 기술 나열로 낭비하지 않는다.
- `핵심 역량`은 공고가 요구하는 핵심 역량을 원천 근거가 있는 사실로 자연스럽게 어필하는 섹션이다. JD 요구와 Notion/사용자 정정/코드·README 근거가 동시에 있을 때만 쓰고, 근거가 없거나 약하면 쓰지 않는다. 부족한 역량을 섹션명이나 키워드로 채우지 말고, 섹션을 줄이거나 제외한다.
- `핵심 역량`의 라벨과 첫 문장은 추상적인 역량어가 아니라 면접관이 바로 질문할 수 있는 기술 단위로 쓴다. `AI·제품 연결`, `서비스 구현력`, `제품 구현력`, `클라우드·업무 플랫폼 구현`처럼 넓고 AI스러운 라벨은 금지한다.
- AX/AI/FDE 공고의 핵심역량은 원천 DB의 구체 사실을 우선해 `다중 Agent 업무지원 설계`, `장시간 AI 작업 운영화`, `Agent 응답 UI 구현`, `업무 플랫폼 기능 구현`, `AWS 서버리스 연동`, `데이터 조회·관측 흐름 개선`처럼 쓴다. 각 불릿에는 Master/Supervisor/OpenSearch/Graph Agent, Agent Instruction, Bedrock Agent Trace, Amazon SQS, AWS Lambda, Amazon API Gateway WebSocket APIs, DynamoDB 커서 페이지네이션, DOMPurify, API Key 서버 분리 등 실제 한 일의 하위 근거를 붙인다.
- 프론트/제품 공고의 핵심역량은 `실시간 협업 상태 처리`, `업무 화면·API 구현`, `검색·하이라이트·메시지 이동 UX`, `프로젝트 분리 영향도 분석`처럼 화면, 상태, API, 데이터 흐름 단위로 쓴다. JD가 요구하지 않는 AI/AWS/DB 키워드는 첫 페이지에서 줄인다.
- 핵심역량 작성 절차는 `JD 자격요건/우대사항 표현 추출 -> 원천 DB/사용자 최신 정정에서 직접 근거 찾기 -> 사용자가 준 좋은 업무 표현 후보에서 맞는 것 선별 -> 문제/제약, 내가 한 판단·구현, 결과/개선 순서로 한 문장화`다. 공고 문구에도 없고 원천 근거도 약한 표현은 쓰지 않는다.
- 사용자가 준 디지엠유닛원 공통 역량 표현은 AI/AX/FDE/자동화/개발 생산성 공고에서 후보로 쓴다. 예: `AI를 개발 프로세스에 도입해 퍼블리싱, 기술 조사, PoC, 문서화 과정을 표준화`, `Plan-First Workflow`, `Human Approval Gate`, `Researcher·Planner·Reviewer 역할 분리`, `프롬프트 및 로깅 체계`, `반복 퍼블리싱 작업 시간 4시간 -> 30분(87% 단축)`. 단, 제출 문서에서는 공고와 맞는 1개만 짧게 쓰고 과장하지 않는다.
- 사용자가 준 프비티 표현은 프론트엔드/제품/업무 플랫폼/SaaS 공고에서 후보로 쓴다. 예: `수발주·영업관리·계정관리 기능이 결합된 구조에서 기능 간 의존성을 직접 분석`, `36K LOC·300+ 파일·39개 API`, `본사/브랜드 식별자 의존성 제거·치환 범위`, `수발주·영업관리·계정관리 영역을 별도 프로젝트 구조로 나누는 기준`, `고객용 CRM·마케팅 플랫폼 리뉴얼`, `고객 프로모션 기능`, `가맹점주 댓글 응답 자동화`, `반응형 UI 및 크로스브라우징 대응`.
- 사용자가 준 CPPM 표현은 AI/AX/FDE/풀스택/AWS/업무 자동화 공고에서 핵심 후보로 쓴다. 예: `Agent Trace 기반 실시간 작업 단계 UI`, `싱글톤 기반 AgentEventManager`, `requestAnimationFrame 기반 렌더링 제어`, `debounce 300ms`, `LastEvaluatedKey 기반 페이지네이션`, `Lambda 환경변수 기반 API Key 격리`, `DOMPurify 기반 Sanitization`, `Master -> Supervisor -> OpenSearch·Graph Agent 3계층 Multi-Agent 아키텍처`, `Amazon Bedrock Knowledge Base(RAG)`, `CloudWatch 로그 분석`, `Graph Agent 기반 서버리스 시각화 파이프라인`.
- 사용자가 준 긴 CPPM 불릿은 그대로 다 넣지 않는다. 같은 의미가 반복되는 항목은 하나로 합치고, 지원 공고의 자격요건/우대사항과 맞는 3~5개만 선별한다. 예를 들어 `보안 취약점 제거`와 `API Key 격리·DOMPurify XSS 방어`는 하나로 합치고, `DynamoDB debounce/페이지네이션` 중복도 하나만 쓴다.
- `ERD 설계부터 RESTful API 설계 및 개발까지 전담`처럼 강한 표현은 사용자 최신 정정으로 확인된 범위에서만 쓴다. 공개 문서에서는 `RESTful API`처럼 올바른 표기를 사용한다.

## Analysis Contract

`company-jd-analyzer` 결과는 아래 12개 섹션을 유지한다.

1. 공고 핵심 요약
2. 회사/서비스 개요
3. 최근 6~12개월 주요 시그널
4. JD에서 명시된 핵심 요구사항
5. JD에서 읽히는 숨은 요구사항
6. 이 회사가 원하는 사람상
7. 이력서에 강조해야 할 사인
8. 우선 배치할 경험/프로젝트
9. 지원자 입장에서의 리스크
10. 예상 면접 질문
11. 총평
12. 근거 출처 메모

## Strategy Contract

`cv-jd-tailor`의 지원전략 문서는 아래 섹션을 유지한다.

1. `# JD 매칭 요약`
2. `# 맞춤 이력서`
3. `# 맞춤 포트폴리오`
4. `# 조정 메모`
5. `# 지원회사 관리 메모`

전략 문서에는 JD 키워드와 원천 경험 매핑, 강조할 카드, 축소할 요소와 이유, 남은 리스크가 보여야 한다.

전략 문서는 최종 산출물의 계약이다. 이력서와 포트폴리오의 상단 문구, 핵심 역량, 경력 불릿 순서, 프로젝트 선택은 반드시 채용분석 문서의 `4. JD에서 명시된 핵심 요구사항`, `5. JD에서 읽히는 숨은 요구사항`, `6. 이 회사가 원하는 사람상`, `7. 이력서에 강조해야 할 사인`, `8. 우선 배치할 경험/프로젝트`로 설명 가능해야 한다.

Notion 원천에 좋은 경험이 있어도 채용분석과 직접 맞지 않으면 축소하거나 제외한다. 분석의 `[확인필요]` 항목은 최종 문서에서 확정 사실처럼 쓰지 않는다.

## Status Model

`db/지원회사_관리.md`의 전형 상태는 아래 목록에서 고른다.

- `서류 준비`
- `서류 제출`
- `서류 탈락`
- `과제`
- `과제 탈락`
- `코테`
- `코테 탈락`
- `면접 1차`
- `면접 1차 탈락`
- `면접 2차`
- `면접 2차 탈락`
- `최종 합격`
- `최종 탈락`
- `보류`

중복 기준은 `회사명 + 직무명 + JD URL`이다. JD URL이 없으면 `회사명 + 직무명 + JD text hash 또는 JD file path`를 사용한다.

## Verification

구조 변경, 스킬 변경, 템플릿 변경, PDF 산출물 생성 후에는 변경 파일과 남은 리스크를 최종 응답에 짧게 보고한다.

지원 전 산출물 생성 후 가능한 확인:

- Notion `박건우 | Developer` 원천 페이지 접근 또는 최신 내용 확인
- Notion 접근이 불가능해 로컬 스냅샷을 썼다면 `db/이력서_원천.md`, `db/포트폴리오_원천.md`의 기준 시점과 한계를 보고
- `db/지원회사_관리.md` 존재
- 분석 문서 12섹션 존재
- 지원전략 문서 존재
- 이력서/포트폴리오 Typst 파일 존재
- PDF를 만들었다면 `typst compile`이 성공했는지 확인
- 지원회사 관리 DB에 같은 application key가 중복되지 않았는지 확인
- 최종 이력서/포트폴리오의 선택과 순서가 채용분석 4~8번 섹션과 지원전략으로 설명 가능한지 확인
- 경력 업무가 `주요 프로젝트`에 중복 배치되지 않았는지 확인
- 라이브러리·프레임워크·클라우드 서비스가 제공한 기능과 내가 직접 구현한 범위가 구분되어 있는지 확인
- 프로젝트가 프로젝트별로 구분되어 있고, SKALA가 주요 프로젝트처럼 과장되지 않았는지 확인
- 교육/활동 항목이 현재 JD에 어필되는 기술 내용만 담고, 항목당 불릿 2개 이하인지 확인
- 엘리스와 스마일게이트 윈터데브가 기본 제외됐는지 확인
- 자격증/어학은 JD 명시 요구가 있을 때만 들어갔는지 확인
- 제출용 문서에 `제외 기술` 같은 방어적 메타 섹션이 없는지 확인
- 수상은 실제 수상만 날짜와 함께 시간순 불릿로 나열했고, 크래프톤 정글 우수 프로젝트 선정은 제외했는지 확인
- 사용자 최신 정정이 회사 산출물과 스킬/지침에 반영됐는지 확인

`scripts/preflight-application.mjs`가 없는 현재 상태에서는 수동 검증을 수행한다. 스크립트를 다시 만들면 이 섹션에 명령을 추가한다.
