# Codex Project Guide

이 저장소는 JD 맞춤 지원서 자동화 작업 공간이다.

목표는 JD 하나를 입력하면 `분석 -> 전략 -> 맞춤 이력서/포트폴리오 -> PDF` 흐름을 같은 기준으로 반복하는 것이다. Codex 공식 문서 기준 프로젝트 지침은 루트 `AGENTS.md`에 두고, 세부 작업 절차는 `.agents/skills/*/SKILL.md`에 둔다.

## Git Tracking Policy

- GitHub에는 스킬, `.gitignore`, `AGENTS.md`, 공개 템플릿만 올린다.
- `db/`, `workflow/`, `assets/`는 개인정보와 지원 산출물을 담는 로컬 전용 경로이므로 Git에 올리지 않는다.
- 원천 DB와 지원 현황은 로컬에서는 계속 사용하지만 버전관리 대상이 아니다.

## Canonical Files

- 이력서 원천 DB: `db/이력서_원천.md`
- 포트폴리오 원천 DB: `db/포트폴리오_원천.md`
- 지원회사 관리 DB: `db/지원회사_관리.md`
- 공용 포트폴리오 자산: `assets/portfolio/`
- 이력서 기준 템플릿: `templates/public/resume.typ`
- 포트폴리오 기준 템플릿: `templates/public/portfolio.typ`
- JD 분석 스킬: `.agents/skills/company-jd-analyzer/SKILL.md`
- 지원서 생성 스킬: `.agents/skills/cv-jd-tailor/SKILL.md`

## Core Workflow

1. JD를 먼저 읽고 회사명, 직무명, JD 식별자를 확정한다.
2. `company-jd-analyzer`로 회사/JD 분석을 수행한다.
3. 분석 결과는 `workflow/지원전/{회사명}/{회사명}_채용분석.md`에 둔다.
4. `cv-jd-tailor`로 원천 DB를 읽고 지원전략, Typst, PDF를 생성한다.
5. 지원 전 산출물은 `workflow/지원전/{회사명}/`에 둔다.
6. 지원 후 보관본은 사용자가 명시하거나 상태 변경이 필요한 경우에만 `workflow/지원후/{회사명}/`에 둔다.
7. 지원 상태는 `db/지원회사_관리.md`에서 중복 없이 갱신한다.
8. 지원 현황은 현재 상태만 관리한다. 변경 이력 섹션이나 별도 상태 로그는 만들지 않는다.

## Ground Rules

- 경험 사실, 수치, 역할, 성과는 `db/이력서_원천.md`, `db/포트폴리오_원천.md`, 사용자 최신 정정만 근거로 사용한다.
- DB는 최종 문장이 아니라 재료다. JD에 맞춰 표현, 순서, 강조점은 바꾸되 사실관계와 인과관계는 새로 만들지 않는다.
- 서로 다른 경험 사실을 임의 인과관계로 합치지 않는다.
- 사용자가 명시하지 않은 동기, 문제의식, 의도, 신념을 이력서/포트폴리오 문장으로 만들지 않는다. 특히 `~라는 문제의식으로`, `~하겠다는 의도로` 같은 표현은 원천 DB나 사용자 최신 정정에 근거가 있을 때만 쓴다.
- 직접 경험이 없는 기술명은 보유처럼 쓰지 않는다.
- JD의 `담당업무`, `자격요건`, `우대사항` 문구를 우선하되, 실제 경험과 의미가 맞는 범위에서만 사용한다.
- 분석 문서의 사실/추론 라벨(`[명시]`, `[최근근거]`, `[추정]`, `[확인필요]`)은 전략 문서에도 유지한다.
- `workflow/지원후/`는 제출·보관본이다. 사용자가 명시적으로 요청한 경우에만 수정하거나 삭제한다.
- `workflow/지원전/`은 작업 산출물 위치다.
- 산출물 파일명은 `{회사명}_{산출물명}_{지원자명}` 형식을 기본으로 한다.
- Typst 산출물은 `.typ`와 `.pdf`를 기본으로 한다.

## Folder Policy

- 회사별 맞춤 산출물은 회사 폴더에 둔다.
- 반복 사용 가능한 사실은 회사 폴더에 복제하지 말고 `db/`에 남긴다.
- 반복 사용 가능한 포트폴리오 이미지는 `assets/portfolio/`에 둔다.
- 회사별로만 쓰는 제출 첨부파일은 해당 회사 폴더에 둔다.
- `workflow/outputs/`나 분기별 `{YYYY-qN}/` 경로는 쓰지 않는다. 현재 기준 경로는 `workflow/지원전`과 `workflow/지원후`다.

## Portfolio Assets

공용 포트폴리오 이미지는 아래 위치를 사용한다.

- 아키텍처 이미지: `assets/portfolio/architecture/`
- 컴포넌트 이미지: `assets/portfolio/components/`
- 화면 캡처: `assets/portfolio/screenshots/`
- 흐름/시퀀스/프로세스 도식: `assets/portfolio/diagrams/`
- 로고: `assets/portfolio/logos/`

이미지를 포트폴리오에 사용할 때는 같은 이미지를 회사 폴더마다 복사하지 않는다. 생성된 Typst 파일에서는 회사 폴더 기준 상대 경로를 사용한다.

## Template Rules

- 이력서 기본 템플릿의 단일 기준은 `templates/public/resume.typ`다.
- 포트폴리오 기본 템플릿의 단일 기준은 `templates/public/portfolio.typ`다.
- `workflow/지원전/_shared_application.typ`와 `workflow/지원전/_fixed_portfolio_cases.typ`가 있으면 기존 산출물 호환용으로 사용할 수 있다.
- 회사별 맞춤은 내용, 순서, 강조점, 불릿 선별로 처리한다.
- 기본 레이아웃을 바꿀 때는 템플릿 파일을 수정하고, 회사별 산출물마다 레이아웃을 새로 만들지 않는다.
- 이력서에서 재직 중 수행한 회사 업무는 `경력` 섹션에만 녹인다. 같은 내용을 `주요 프로젝트`에 중복 배치하지 않는다.
- 한 회사 경력 안에 여러 제품, 프로젝트, 업무영역이 섞여 있으면 경력 항목 내부에서 작은 소제목으로 나눈다. 회사 업무를 `주요 프로젝트`로 빼지 않는다.
- 디지엠유닛원 경력 내 프로젝트 구분은 `프비티`와 `LG 공통업무 플랫폼` 두 개를 기준으로 한다. AI Native 워크플로우는 별도 프로젝트로 세우지 않고 실제 수행 프로젝트 안의 업무 방식/성과로 녹인다.
- `주요 프로젝트` 섹션은 경력 외 개인 프로젝트, 교육 과정 프로젝트, 연구/캡스톤 등 일 외 산출물만 배치한다.
- 디지엠유닛원 경력 역할명은 JD에 맞춰 조정한다. 프론트엔드 공고는 `Frontend Engineer`, 풀스택/AI Native/AX/AI 서비스/PI/Builder 성격 공고는 `Software Engineer`로 쓴다. 이 규칙은 디지엠유닛원 재직 경력에만 적용하며, 크래프톤 정글 Code Sync는 프론트엔드 프로젝트이므로 `Frontend Developer`로 둔다.
- 이력서 경력 불릿은 소개나 서브타이틀에 이미 나온 담당 범위를 반복하지 않는다. 문제, 실행, 성과/수치/도메인 임팩트가 없는 문장은 소개·서브타이틀 맥락으로만 두거나 제거한다.

## Analysis Contract

`company-jd-analyzer` 결과는 아래 12개 섹션을 유지한다.

1. 공고 핵심 요약
2. 회사/서비스 개요
3. 최근 6~12개월 주요 시그널
4. JD에서 명시된 핵심 요구사항
5. JD에서 읽히는 숨은 요구사항
6. 이 회사가 원하는 사람상
7. 이력서에 강조해야 할 사인
8. 우선 배치할 경험/프로젝트
9. 지원자 입장에서의 리스크
10. 예상 면접 질문
11. 총평
12. 근거 출처 메모

## Strategy Contract

`cv-jd-tailor`의 지원전략 문서는 아래 섹션을 유지한다.

1. `# JD 매칭 요약`
2. `# 맞춤 이력서`
3. `# 맞춤 포트폴리오`
4. `# 조정 메모`
5. `# 지원회사 관리 메모`

전략 문서에는 JD 키워드와 원천 경험 매핑, 강조할 카드, 축소할 요소와 이유, 남은 리스크가 보여야 한다.

전략 문서는 최종 산출물의 계약이다. 이력서와 포트폴리오의 상단 문구, 핵심 역량, 경력 불릿 순서, 프로젝트 선택은 반드시 채용분석 문서의 `4. JD에서 명시된 핵심 요구사항`, `5. JD에서 읽히는 숨은 요구사항`, `6. 이 회사가 원하는 사람상`, `7. 이력서에 강조해야 할 사인`, `8. 우선 배치할 경험/프로젝트`로 설명 가능해야 한다.

DB에 좋은 경험이 있어도 채용분석과 직접 맞지 않으면 축소하거나 제외한다. 분석의 `[확인필요]` 항목은 최종 문서에서 확정 사실처럼 쓰지 않는다.

## Status Model

`db/지원회사_관리.md`의 전형 상태는 아래 목록에서 고른다.

- `서류 준비`
- `서류 제출`
- `서류 탈락`
- `과제`
- `과제 탈락`
- `코테`
- `코테 탈락`
- `면접 1차`
- `면접 1차 탈락`
- `면접 2차`
- `면접 2차 탈락`
- `최종 합격`
- `최종 탈락`
- `보류`

중복 기준은 `회사명 + 직무명 + JD URL`이다. JD URL이 없으면 `회사명 + 직무명 + JD text hash 또는 JD file path`를 사용한다.

## Verification

구조 변경, 스킬 변경, 템플릿 변경, PDF 산출물 생성 후에는 변경 파일과 남은 리스크를 최종 응답에 짧게 보고한다.

지원 전 산출물 생성 후 가능한 확인:

- `db/이력서_원천.md` 존재
- `db/포트폴리오_원천.md` 존재
- `db/지원회사_관리.md` 존재
- 분석 문서 12섹션 존재
- 지원전략 문서 존재
- 이력서/포트폴리오 Typst 파일 존재
- PDF를 만들었다면 `typst compile`이 성공했는지 확인
- 지원회사 관리 DB에 같은 application key가 중복되지 않았는지 확인
- 최종 이력서/포트폴리오의 선택과 순서가 채용분석 4~8번 섹션과 지원전략으로 설명 가능한지 확인
- 경력 업무가 `주요 프로젝트`에 중복 배치되지 않았는지 확인
- 교육/활동 항목이 현재 JD에 어필되는 내용만 담고, 항목당 불릿 2개 이하인지 확인
- 사용자 최신 정정이 회사 산출물뿐 아니라 관련 `db/` 원천 파일에도 반영됐는지 확인

`scripts/preflight-application.mjs`가 없는 현재 상태에서는 수동 검증을 수행한다. 스크립트를 다시 만들면 이 섹션에 명령을 추가한다.
