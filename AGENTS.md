# Codex Project Guide

이 저장소는 JD를 분석하고, 검증된 경력 원천에서 맞춤 이력서·포트폴리오를 생성하는 작업 공간이다.

## Instruction Boundary

- 이 파일은 저장소 전체에 적용되는 공통 작업 경계와 검증 규칙만 정의한다.
- JD 분석, 경험 선택, 불릿 구성, 핵심역량, 자기소개서, 포트폴리오 작성 규칙은 `.agents/skills/company-jd-analyzer/SKILL.md`와 `.agents/skills/cv-jd-tailor/SKILL.md`에서 관리한다.
- 공통 규칙과 스킬 규칙이 중복되거나 충돌하면 이 파일의 출처·사실성·보관 경계를 우선하고, 세부 작성 방식은 해당 스킬을 따른다.
- 사용자가 잘못된 기술명·역할·수치·인과관계를 요청해도 그대로 반영하지 않는다. 원천 근거를 확인하고, 확인되지 않으면 제외하거나 확인 필요로 낮춘 뒤 이유를 기록한다.
- 요청 범위가 어느 문서의 역할인지 먼저 판정한다. 저장소 정책은 이 파일, JD별 생성 절차는 스킬, 재사용 사실은 Notion 원천에 둔다.

## Source Of Truth

- 재사용 가능한 이력서·포트폴리오 사실의 1차 원천은 Notion `박건우 | Developer` 페이지다.
- 로컬 `db/이력서_원천.md`와 `db/포트폴리오_원천.md`는 Notion 접근이 불가능할 때만 fallback으로 사용한다. Notion과 충돌하면 Notion과 사용자의 최신 정정을 우선한다.
- 사실, 역할, 수치, 성과, 개인 기여를 서로 다른 프로젝트에서 임의로 결합하지 않는다.
- 라이브러리·프레임워크·클라우드 서비스의 기본 기능과 사용자가 직접 작성·연동·검증한 범위를 구분한다.
- 팀 전체의 결과, 확인되지 않은 기술, 추정 수치, 계획·초안·검토 항목을 완료된 개인 경험으로 쓰지 않는다.
- 사용자의 최신 정정은 재사용 사실로 반영하되, 기존 사실과 충돌하면 충돌 지점을 확인하고 한 가지 기준으로 정리한다.

## Workflow

1. JD의 회사명·직무명·공고 ID를 확정한다.
2. `company-jd-analyzer`로 JD를 분석하고 결과를 `workflow/지원전/{회사명}_{JD식별자}/`에 둔다.
3. `cv-jd-tailor`로 Notion 원천을 읽어 JD에 맞는 경험만 선별한다.
4. 지원전 산출물은 `workflow/지원전/{회사명}_{JD식별자}/`에 생성한다.
5. 사용자가 지원 완료를 명시한 경우에만 제출 당시 파일을 `workflow/지원후/{회사명}_{JD식별자}/`에 보관한다.

## Output And Archive

- 지원 폴더명은 `{회사명}_{공식 공고 ID 또는 URL 고유 ID}`를 기본으로 한다. 같은 회사의 다른 공고는 별도 폴더를 사용한다.
- `workflow/지원후/`는 제출 당시의 불변 보관본이다. 지원 완료 후 파일을 수정·재생성·덮어쓰기·삭제하지 않는다.
- 수정이 필요하면 Notion 원천, 공용 지침, 또는 `workflow/지원전/`에 반영하고 새 지원 시 새 산출물을 만든다.
- 반복 사용 가능한 사실은 회사 폴더에 복제하지 않고 Notion 또는 `db/`에 둔다.
- 공용 포트폴리오 자산은 `assets/portfolio/`에 두며 회사별로 복사하지 않는다.
- GitHub에는 공용 지침, 스킬, 공개 템플릿만 올린다. `db/`, `workflow/`, `assets/`는 로컬 전용이다.
- Typst 파일과 PDF는 같은 지원전 폴더에 둔다. PDF는 컴파일 성공을 확인한 경우에만 존재한다고 보고한다.

## Harness Guardrails

- 작업 시작 시 `AGENTS.md`와 호출할 스킬의 범위를 확인하고, 스킬이 요구하는 원천·분석·검증 단계를 생략하지 않는다.
- 생성 전에는 JD 요구사항과 원천 근거를 매핑하고, 생성 후에는 선택 경험·수치·기술·역할·제외 항목을 다시 대조한다.
- 문서에 들어가는 모든 핵심 주장은 원천 페이지, 사용자 최신 정정, 코드·README·실행 결과 중 하나로 추적 가능해야 한다.
- 한 불릿은 하나의 문제 또는 제약, 내가 한 조치, 결과 또는 산출물만 담는다. 서로 다른 프로젝트의 사실을 하나의 성과처럼 연결하지 않는다.
- 핵심역량은 보통 4개 안팎으로 제한한다. 각 항목은 한 문장으로 대표 역량·구현 범위·대표 사례만 요약하고, 세부 기술·문제 해결 과정·수치는 경력·프로젝트 불릿에서 증명한다. 핵심역량이 상세 이력처럼 길어지거나 같은 내용을 반복하면 줄인다.
- 한줄소개는 공고마다 다시 만든다. `다양한 화면`이나 `AX에 기여`처럼 범위만 넓거나 결과가 추상적인 고정 문구를 피하고, 해당 JD의 핵심 요구와 여러 경험에서 반복적으로 확인된 구현 단위를 연결한다. 프론트엔드 JD에서는 예를 들어 `React·Next.js 기반으로 다양한 사용자 흐름을 화면·API·상태로 구현해 온 개발자`처럼 공고의 핵심 기술을 앞세우고, AI/AX JD에서는 검증된 Agent·업무 흐름 근거를 앞세운다.
- 사용자 경험을 설명할 때 성공 상태만 나열하지 않는다. 로딩·빈 결과·오류·장시간 처리·탐색 상태 중 실제로 고려하고 구현한 범위를 함께 드러내며, `상태를 검증했다`보다 사용자에게 어떤 상태를 보여 주고 다음 행동을 가능하게 했는지와 화면·이벤트·API 구현 근거를 쓴다.
- 기술명만 나열한 문장, 근거 없는 JD 키워드, 과장된 도메인 운영 경험, 제출 문서에서의 방어적 메타 문구를 제거한다.
- 모든 제출 문장은 단순 기술 목록이 아니라 사실 기반의 짧은 스토리로 구성한다. 먼저 누가 어떤 문제·제약·업무 맥락을 겪었는지 쓰고, 그 상황에서 본인이 내린 판단과 구현·연동·검증을 연결한 뒤, 수치·사용자 변화·오류 처리·완성된 산출물 중 확인 가능한 결과로 끝낸다. 근거 없는 동기·인과·성과는 만들지 않는다.
- CPPM에서 Trace/progress UI의 결과와 처리시간 단축 결과를 구분한다. Trace UI는 사용자가 정상 진행 여부를 확인하게 해 체감 대기 부담과 이탈 문의 감소에 기여한 결과로 쓰고, 평균 응답시간 약 2분에서 1분 10초로 줄인 결과는 프롬프트 경량화·Agent 흐름 조정의 별도 결과로 쓴다. Trace UI 자체가 처리시간을 줄였다고 쓰지 않는다.
- 산출물 경로, 지원 완료 여부, PDF 생성 여부를 혼동하지 않는다. `workflow/지원후/`의 존재만 제출 보관 신호로 사용한다.

## Validation

- Notion 원천 접근 여부와 fallback 사용 여부를 기록한다.
- 분석 문서가 존재하고 회사명·직무·공고 식별자·12개 분석 섹션을 포함하는지 확인한다.
- 최종 문서의 모든 선택 경험이 JD 분석 및 원천 근거와 연결되는지 확인한다.
- 회사 업무가 독립 프로젝트에 중복되지 않고, 교육·활동·수상·병역·학력이 별도 섹션으로 유지되는지 확인한다.
- `git diff --check`를 실행한다.
- Typst를 사용하는 경우 `typst compile`을 실행하고 PDF 생성 여부를 확인한다. 폰트 대체 같은 환경 경고와 컴파일 실패를 구분해 보고한다.
- 스킬을 수정한 경우 해당 스킬의 검증 스크립트를 실행한다.

## Canonical Paths

- Notion 원천: `https://confused-dietician-c17.notion.site/Developer-3c77caa087bd80ff9d73f63aeeebb4d7?pvs=74`
- JD 분석 스킬: `.agents/skills/company-jd-analyzer/SKILL.md`
- 지원서 생성 스킬: `.agents/skills/cv-jd-tailor/SKILL.md`
- 로컬 원천 fallback: `db/이력서_원천.md`, `db/포트폴리오_원천.md`
- 이력서 템플릿: `templates/public/resume.typ`
- 포트폴리오 템플릿: `templates/public/portfolio.typ`
